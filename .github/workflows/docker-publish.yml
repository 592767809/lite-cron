name: Build and Push Docker Image to GHCR

on:
  # 当有 v* 标签推送时触发（仅在创建 tag 时，分支提交不触发）
  push:
    tags:
      - 'v*'
  # 当发布 release 时触发
  release:
    types: [published]
  # 手动触发
  workflow_dispatch:
    inputs:
      tag:
        description: '镜像标签 (默认: latest)'
        required: false
        default: 'latest'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史以便检查分支

      - name: 验证 tag 来源
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "🏷️ Tag: $TAG_NAME"

          # 检查 tag 是否在 main 分支上
          git fetch origin main
          if git branch -a --contains $TAG_NAME | grep -q "remotes/origin/main"; then
            echo "✅ Tag 来自 main 分支，继续构建"
          else
            echo "⚠️ 警告: Tag 可能不是来自 main 分支"
            echo "包含此 tag 的分支:"
            git branch -a --contains $TAG_NAME
          fi

      - name: 设置 QEMU
        uses: docker/setup-qemu-action@v3

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录到 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 调试事件信息
        run: |
          echo "🔍 事件类型: ${{ github.event_name }}"
          echo "🔍 Ref: ${{ github.ref }}"
          echo "🔍 Ref 类型: ${{ github.ref_type }}"
          echo "🔍 Release Tag: ${{ github.event.release.tag_name || 'N/A' }}"

      - name: 提取元数据
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          # 生成标签：tag名称 + 语义化版本 + latest
          tags: |
            # 1. Tag 名称 (如 v1.0.0)
            type=ref,event=tag
            # 2. 语义化版本标签 (v1.0.0 -> 1.0.0)
            type=semver,pattern={{version}}
            # 3. 始终生成 latest
            type=raw,value=latest,enable=true
          # 禁用默认的 sha 和 branch 标签
          flavor: |
            latest=auto

      - name: 显示生成的标签
        run: |
          echo "🏷️ 生成的标签:"
          echo "${{ steps.meta.outputs.tags }}" | tr ',' '\n'
          echo ""
          echo "📋 生成的标签 (JSON):"
          echo '${{ steps.meta.outputs.json }}' | jq .

      - name: 构建并推送镜像
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 注意：暂时禁用工件证明，因为它会推送额外的 sha256 标签到 registry
      # 如需启用，请确保配置正确或使用 GitHub 的默认证明存储
      # - name: 生成工件证明
      #   uses: actions/attest-build-provenance@v1
      #   with:
      #     subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      #     subject-digest: ${{ steps.push.outputs.digest }}
      #     push-to-registry: false

      - name: 输出镜像信息
        run: |
          echo "✅ 镜像构建并推送成功！"
          echo ""
          echo "镜像地址: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          echo "标签: ${{ steps.meta.outputs.tags }}"
          echo "Digest: ${{ steps.push.outputs.digest }}"
          echo ""
          echo "拉取命令:"
          echo "  docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
